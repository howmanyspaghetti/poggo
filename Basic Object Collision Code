if x!=0
if !place_free(x+x,y)
{
	if x>0 move_contact_solid(0,x)
	if x<0 move_contact_solid(180,-x)
	x=0
} 

if y!=0
if place_free(x+x,y+y)
{
	if y>0 move_contact_solid(270,y)
	if y<0 move_contact_solid(90,-y)
	y=0
}
